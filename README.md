# Caesar-Cipher
A simple Python-based Caesar Cipher program that can encode and decode messages with user-defined shift values. Handles non-alphabet characters and supports continuous user interaction.
